services:
  osrm:
    image: ghcr.io/project-osrm/osrm-backend
    container_name: osrm-8states
    ports:
      - "5555:5000"
    volumes:
      - ./data:/data
    command: osrm-routed --algorithm mld /data/8states.osrm
    restart: unless-stopped
